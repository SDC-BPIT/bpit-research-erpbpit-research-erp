DATABASE_URL="postgresql://postgres:password@localhost:5432/bpit_research"
JWT_SECRET="bpit_erp_jwt_secret_2024"

# SMTP Email Configuration
# Instructions:
# 1. For Gmail, use smtp.gmail.com, port 587, and an App Password (not your normal password).
# 2. For Outlook/Office365, use smtp.office365.com, port 587.
# 3. If using an external SMTP service (like SendGrid or Mailgun), fill in their details below.
SMTP_HOST="smtp.office365.com"
SMTP_PORT="587"
SMTP_SECURE="false" # false for port 587 (TLS), true for port 465 (SSL)
SMTP_USER="amandureja@bpitindia.edu.in"
SMTP_PASS="wbxdvnjfcxvklcvp"
EMAIL_FROM="BPIT ERP <amandureja@bpitindia.edu.in>"



DATABASE_URL="postgresql://postgres:password@localhost:5432/bpit_research"
JWT_SECRET="bpit_erp_jwt_secret_2024"

# SMTP Email Configuration
# Instructions:
# 1. For Gmail, use smtp.gmail.com, port 587, and an App Password (not your normal password).
# 2. For Outlook/Office365, use smtp.office365.com, port 587.
# 3. If using an external SMTP service (like SendGrid or Mailgun), fill in their details below.
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_SECURE="false" # false for port 587 (TLS), true for port 465 (SSL)
SMTP_USER="amandureja@gmail.com"
SMTP_PASS="fesm snpk htwb uyqk"
EMAIL_FROM="BPIT ERP <amandureja@gmail.com>"